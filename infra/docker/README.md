# Docker Notes

This folder is reserved for Docker-related infrastructure material.

## Current Status

Day Zero uses a root-level `docker-compose.yml` and an API Dockerfile.

## Future Improvements

- production-ready API container command
- multi-stage frontend container
- environment-specific compose overlays
- health checks and startup ordering
- migration entrypoint scripts
